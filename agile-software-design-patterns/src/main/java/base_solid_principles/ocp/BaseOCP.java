package base_solid_principles.ocp;

// OCP 开放原则
// Open Close Principle
// Closed for modification, open for extension 核心在于新的添加，而不是改变原有的
// 1. Each new feature should not modify existing source code
// 2. A component should be extendable to behave in a new ways
public class BaseOCP {

    // Extending its capabilities 如何在原有类型上扩展
    // 1. inheritance 使用继承来重写或者是新增方法
    // 2. strategy pattern
}
